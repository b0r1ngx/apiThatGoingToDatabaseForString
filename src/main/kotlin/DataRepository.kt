class DataRepository() {
    fun getData(s: String): String {
        val result = "Hello, World !"
        // It must be a call to lower interpretation
        // like to specific DB, or a bit lower, like to DAO
        return result
    }
}